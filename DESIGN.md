# Design

Визуальная система прототипа Ключи24. Записана с построенного кода `index.html`.
Направление задано референсом заказчика: светлая, воздушная витрина продукта,
почти монохром с одним тёплым акцентом.

## Каркас

Страница живёт в **скруглённом белом контейнере** (`.shell`, радиус 26px, мягкая тень)
на сером фоне `#EDEDEA`. Переключатель ролей и служебная строка вынесены **наружу** контейнера:
это оснастка демонстрации, а не часть продукта, и она не должна притворяться интерфейсом.
На ширине до 680px контейнер раскрывается во весь экран без скруглений.

## Цвет

Стратегия — сдержанная: нейтральные плюс один акцент. Цвет означает состояние, а не украшает.

| Токен | Значение | Роль |
|---|---|---|
| `--bg` | `#EDEDEA` | фон вокруг контейнера |
| `--shell` / `--white` | `#FFFFFF` | контейнер и карточки |
| `--paper` | `#F6F6F3` | фон рабочих экранов, мягкие панели |
| `--line` / `--line-2` | `#E8E8E2` / `#F1F1EC` | волосяные границы и разделители |
| `--ink` | `#17171C` | текст, тёмные кнопки, иконочные плитки |
| `--ink-2` | `#56565F` | вторичный текст |
| `--ink-3` | `#6A6A75` | подсказки |
| `--accent` | `#C7451C` | акцент, срочное, каретка, счётчик уведомлений |
| `--ok` | `#0B7C4F` | свободно, подтверждение, приход |
| `--info` | `#2C5AD0` | занятость |
| `--warn` | `#8F5A00` | ожидание уборки |
| `--alert` | `#BE3F18` | ремонт, удержание, возврат |

Каждое состояние имеет пастельную подложку (`--*-soft`) для ярлыков-пилюль.
Все **24 боевые пары** «текст на фоне» посчитаны и держат ≥4.5:1. Неактивный текст `#84848D`
даёт 3.3:1 — это служебное состояние, WCAG его не нормирует, но читаемость сохранена.

## Форма

- Радиусы: контейнер 26, карточка 18, поле 12, кнопка и ярлык — пилюля.
- Границы волосяные, 1px. Жирных линий и цветных полос слева нет.
- Тени всегда со смещением и размытием, три ступени (`--sh-s/m/l`); плоских блок-теней нет.
- Кнопка-действие — тёмная пилюля; у крупных есть круглый значок-шеврон справа (`.cap`).

## Шрифт

**Manrope** 400–800, единственная гарнитура. Заголовки: вес 800, строчные,
трекинг −0.028em, у крупных до −0.045em, интерлиньяж 1.06. Мелкие подписи — версалом
с трекингом +0.08em. `font-variant-numeric: tabular-nums` на `body` и полях,
чтобы цифры в шахматке, ценах и суммах не плясали.

## Иконки

Авторский набор из 36 SVG в объекте `ICONS`: сетка 24, штрих 1.9,
`stroke-linecap: round`, заливки нет, цвет наследуется. Рисуются через `ic(name, size)`.
Эмодзи в интерфейсе запрещены.

## Планировки

Шесть квартир показаны авторскими схемами (`PLANS`: `studio`, `k1`, `k2`, `k3`) —
фотографий заказчик не давал. Тёмный контур наружной стены 2.6, светло-серые внутренние
стены, окна прорезаны белым, мебель мягкими блоками, жилые комнаты — тёплой заливкой `#E8CFC3`.
Схема же служит миниатюрой внутри заголовка первого экрана.

## Первый экран

Центрированная колонка 820px: пилюля с числом квартир, заголовок со **встроенной
скруглённой миниатюрой** планировки, лид, сегментный поисковый бар с круглой тёмной кнопкой.
Поиск отсюда переносит выбранные даты и район прямо в кабинет клиента.

## Карта

Светлая схема двух районов: дороги в две обводки (серая подложка и белая жила),
лесопарк заливкой, названия улиц и районов подписаны. Метки — белые пилюли с ценой
и точкой-якорем; занятые приглушены до 45%, выбранная становится тёмной.
При выборе всплывает карточка объекта поверх карты: схема, флажок статуса,
адрес, метраж, вместимость, залог, цена и кнопка брони.

## Движение

Одна грамматика: смена роли или вкладки поднимает содержимое на 9px с затуханием
(`@keyframes wipe`, 0.4s, `cubic-bezier(.16,1,.3,1)`). Модалка всплывает с масштабом,
тост приходит снизу, карточка на карте — с лёгким «поп». Маршрут звонков пульсирует
расходящимся кольцом. Всё гасится `prefers-reduced-motion`.

## Поверхности браузера

Выделение — чернильное, каретка — акцент, полоса прокрутки скруглённая серая с белой
обводкой, фокус — акцентная рамка 2px с отступом, стрелка `select` — встроенный SVG.

## Состояния

`.tag` в шести вариантах с точкой-маркером: `free`, `ok`, `busy`, `clean`, `fix`, `gray`.
Карточки пульта, требующие вмешательства, дополнительно получают мягкую тонировку фона.
Пустые состояния — `.empty` с пунктиром. У кнопок есть `:hover`, `:active`, `[disabled]`;
у полей — `:hover`, `:focus`, `:disabled`.

## Сетка

Контейнер 1280px, содержимое 1180px. Переломы: 1000px (колонки схлопываются, шаги в две
колонки, цифры в две колонки) и 680px (контейнер во весь экран, поисковый бар вертикальный,
таблица документов превращается в карточки). Минимальная высота нажимаемого — 42px,
у мелких кнопок 36px.
